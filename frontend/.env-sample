# Frontend Environment Variables for SkillVerifier

# Backend API URL

VITE_APP_BACKEND_URL=http://127.0.0.1:8000

# GitHub OAuth Configuration

# The app will redirect to /auth/callback after GitHub authorization

VITE_GITHUB_REDIRECT_URI=http://localhost:3000/auth/callback
